# 学校教务排课上下文

本项目服务单校教务团队，覆盖从学期准备、排课发布到学期中调课与代课的完整工作流。本文记录项目中必须保持一致的业务词汇，不记录页面或代码实现细节。

## 使用路径

**首次成功**：排课管理员第一次独立完成并发布一张可用课表的结果。它包括学期准备、基础数据、教学任务、排课检查、课表发布和查询，不等同于完成登录或创建学期。

**常用入口**：面向当前角色、当前工作阶段最常执行的任务入口。常用入口最多保留少量项目，既有角色默认项，也允许用户固定或按最近使用调整；它不是完整功能目录。

**阶段化常用入口**：随首次成功状态和当前学期阶段变化的角色默认入口。首次发布前优先引导建校和排课，首次发布后优先处理课表查询、今日运行和调课代课。

**完整功能**：系统支持的全部业务能力，按学期准备、排课主流程、日常运行、报表和系统管理组织。完整功能不应要求新用户一次性理解。

**首次成功状态**：从首次配置开始持续到第一张课表正式发布的引导状态。设置向导完成只表示基础配置流程结束，不会自动结束首次成功引导。

## 学期与课表

**正式学期**：承载学校真实教学数据并可能对师生发布的学年学期。正式学期中的数据变更需要遵守发布和备份规则。

**示例数据**：仅用于体验和培训的虚构学校数据。示例数据与正式学期是两条明确区分的首次使用路径，不能被误认为真实教学数据。

**当前学期**：学校当前正在进行的唯一工作上下文。仪表盘、常用入口和默认业务操作围绕当前学期展开，其他学期主要用于历史查询或复制。

**教学任务**：描述教师、班级、科目、每周课时以及连堂、协同教学等排课要求的教学安排。

**课表草稿**：尚未对学校正式生效、可以继续检查和编辑的排课版本。

**已发布课表**：经过排课管理员确认并作为学校当前安排使用的课表版本。

**课表发布**：把课表草稿转为学校当前可查询安排的业务动作；发布不等于备份，也不等于审批。

**草稿可见范围**：系统管理员和排课管理员可以编辑课表草稿，教务主任可以查看草稿但不能编辑，教师只使用已发布课表。单张已发布课表可供登录用户查询和导出，批量或版本级导出属于管理操作。

**归档学期**：不再作为当前教学上下文、默认只读的历史学期。归档学期可以查询或复制，但不能进行常规排课写入或发布。

## 角色与权限

**固定角色**：系统管理员、排课管理员、教务主任和教师四种稳定的职责集合。一个用户可以同时拥有多个固定角色。

**管理视角**：拥有排课管理职责的多角色用户登录后的默认工作视角。多角色用户的权限取并集，不通过切换视角临时获得或失去权限。

**排课管理员**：负责维护教学任务、编辑排课草稿、运行排课和发布课表的业务角色。

**教务主任**：负责全校监督和日常教务运行的角色，可以查看全校数据并处理有限的校历、请假、调课与代课事务，但不负责核心排课编辑或课表发布。

**教师**：使用已发布课表、登记本人请假、确认本人通知并查看本人统计的角色，不编辑教学任务或排课草稿。

**动作权限**：对查看、编辑、导入、发布、导出、备份、恢复等具体业务动作的授权，而不仅是能否打开某个页面。

**数据范围**：角色可以作用的数据边界，例如本人、负责范围或全校。教师可以查看已发布的全校课表，但只能修改本人业务数据；教务主任通常使用全校查看范围。

**高风险动作**：会改变学校正式安排、数据安全或账号安全的动作，包括课表发布、删除、备份恢复和账号权限变更。这些动作需要保留确认和审计要求，即使它们出现在常用入口中。

**首次成功条件**：当前正式学期存在一张通过完整性检查、没有必排教学任务遗漏并已发布的课表。示例数据体验成功不替代正式学期的首次成功。
