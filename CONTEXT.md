# 学校教务排课上下文

本项目服务单校教务团队，覆盖从学期准备、排课发布到学期中调课与代课的完整工作流。本文记录项目中必须保持一致的业务词汇，不记录页面或代码实现细节。

## 使用路径

**学期准备**：围绕一个学期逐项建立真实业务数据的工作过程，包括学期与作息时间表、校历、基础数据和班级作息分组。它由排课工作台内的学期管理、校历与准备、资源与导入三个辅助工作面承接，不是独立的完成状态或一次性流程。

**中性学期**：仅包含学年、学期和真实起止日期的学期外壳。创建时不根据学校或班级学段自动写入科目、教师、班级或作息。

**组合基础数据导入**：把科目、教师、班级和教室/场地放在同一工作簿中，先展示新增、未变化、将修改和冲突，再由用户确认并在一个事务中写入。教师登录账号不属于这次导入。

**教师安排模板导入**：把学校来源文档人工整理到系统发布的版本化工作簿后，预览并确认写入科目、教师、班级、教学任务及可选的教室/场地和作息数据。学校原始 Word、Excel 或扫描件不是该流程直接接收的模板。

**来源所有权**：某条业务记录由一个可追溯来源持续维护的归属关系。只有被教师安排模板导入明确接管的记录属于该来源；手工记录不会因为名称相同而改变归属，来源记录的更新或移除必须由用户确认。

**作息分组**：一组共用同一套作息时间表的班级。作息分组可以参考班级的学制标签，但学制标签不决定分组，且同一学制可以拆成多组。

**年级组**：本次班级设置中共享同一年级和学段的班级集合。年级组是批量建立班级时的操作分组，不单独产生可维护的业务实体。

**排课维护唯一入口**：用户可发现的班级、科目和教师档案维护，作息设置、教学任务维护与自动排课工作面位于排课工作台的对应步骤；学期生命周期、校历、特殊日期、资源与导入等准备能力也在同一入口的辅助工作面中承接。侧边栏不再提供三个准备页的独立入口，其他页面不再提供功能完全重复的维护工作面；后端数据和既有批量导入仍由同一业务模型与接口承接。

**排课入口兼容**：旧的学期、校历、基础数据、班级、科目、教师、教学任务、自动排课和作息编辑深链接保留为兼容入口，并重定向到排课工作台的对应步骤或辅助工作面；重定向保留有效的学期、视图、资源分区和作息表上下文，不重新暴露旧维护页面，也不改变既有权限检查。所有相关空状态和快捷动作同样指向工作台，非重复的独立业务入口保持原有目标。

**班级导入兼容**：模板、组合基础数据和参考文件导入继续接收班级记录，并沿用各自的预览、权限、来源归属和审计边界。导入写入的班级与手工班级使用同一学期模型，统一在“设置班级”中查看和编辑；导入不是第二个可发现的班级维护入口。

**批量班级设置**：先按年级组生成班级候选清单，再逐行修订并一次性确认写入。候选清单存在错误或冲突时，不形成部分保存结果。

**班级候选草稿**：一个年级组内年级和学段固定；用户可修改完整班级名称或删除候选行，并按需逐行补充人数、班主任和中职专业。作息表配置属于后续的“设置课时”步骤。

**班级批量提交**：服务端先预览并校验候选草稿，确认时在一次事务中重新校验和写入。预览失效或任一行存在阻断错误时不写入；相同操作的重试返回已有结果，不重复创建班级。

**年级称谓**：新建批量班级时按学段解释数字年级：小学使用 1–6（一年级至六年级），初中使用 7–9（七年级至九年级），普通高中和综合高中使用 10–12（高一至高三），中职使用 1–3（中职一年级至中职三年级）。该映射用于输入校验和默认班级名称。

**班级名称**：同一学期内唯一的班级称谓。批量班级设置默认由年级称谓、班号和“班”组成，用户可以在确认前修改。

**排课前置检查**：在自动或手动排课前，根据当前学期的教学任务、教师、班级、教室/场地和作息数据计算的必要条件报告。报告区分阻断错误和提醒：没有班级、没有可用常规课时、教学任务缺教师或班级、走班结构不一致、硬规则无候选位置，以及普通模式下无可用资源或可拆分的资源总量不足属于错误；教师目标课时超额、教室容量不足和软约束未满足属于提醒。普通模式的错误必须先修正；提醒可继续，但要在发布检查中由有权限的负责人明确复核。

**部分排课**：用户明确选择后允许把可拆分的班级、教师或教室/场地总量不足转为未排课时，生成结果仍是可检查的草稿。缺少必要对象、无可用作息、走班结构不一致、硬规则无候选位置或零资源等结构性错误不能通过部分排课绕过；未排课时及其原因必须保留。常规发布被未排课程阻断，只有教务主任完成明确复核后才能例外发布，并保留未排清单和原因。

**排课准备**：为排课整理教学任务、教师、班级、教室/场地、作息等输入并处理前置问题的业务阶段。它是内部业务语义，不是排课工作台中的独立页面或必须确认的流程节点。

**排课默认规则**：班级、教师和教室/场地同一时段不冲突、教学任务周课时守恒等内置硬规则始终生效；有激活版本时，当前激活的规则集和学期软约束配置用于本次排课，没有激活版本时使用内置规则和默认软约束。规则草稿不会因开始排课自动激活；部分排课也不能关闭物理冲突和结构性约束。

**排课就绪**：数据完整性和求解预检没有阻断错误后的当前学期状态，提醒不阻止就绪确认。排课工作台的“开始排课”动作沿用自动确认就绪的行为；需要明确确认或撤销时，教务主任在工作台的“校历与准备”辅助工作面操作。所有入口使用同一检查、状态和审计记录。模板导入会使该状态回到待确认；确认就绪不激活排课规则、不创建课表草稿，也不表示课表已经发布。

**排课工作台流程**：每次排课都可以使用的五个步骤：设置班级、设置课时、科目节数、教师任课、开始排课。其中“科目节数”和“教师任课”步骤分别提供科目档案/科目节数、教师档案/教师任课两个工作视图；科目档案视图提供常用科目快捷添加，可逐项选择并确认写入当前学期，快捷项不会按学段自动成套创建，且始终使用同一科目数据接口。步骤状态由已有业务事实推导，不区分首次和后续排课，也不保存独立流程进度；生成草稿后进入课程表调整，再执行发布、打印和导出。

**排课工作台**：承载排课工作台流程的统一入口。它在同一入口中嵌入五个步骤对应的业务视图，并在科目与教师步骤内提供档案维护和排课输入的切换，同时以辅助工作面承接学期日期与作息、校历特殊日期及明确就绪确认、教室/场地、模板与参考文件导入和教师账号绑定；这些工作面不保存第二套数据或流程状态。它是首张课表的默认排课入口，生成后的课程表调整和版本与发布属于同一主流程的收尾入口。

**独立业务入口**：排课规则、课程表调整、版本与发布、课表查询等与五步工作台不完全重复的职责继续保留各自页面；学期生命周期、校历、导入、教室/场地和教师登录账号绑定以辅助工作面承接，旧地址仅作兼容重定向。教学任务、自动排课以及班级、科目、教师和作息的重复维护页面不再作为独立业务目的地；共享组件仍可嵌入工作台，代码复用不构成第二个用户入口。

**排课导航承接**：从重复页面的旧深链接进入时，按“排课入口兼容”重定向并保留有效上下文；从仍然独立的规则、导入或结果页面进入时，页面继续完成自身职责，并按上下文提供返回排课工作台的动作。

**排课状态同步**：五步状态、排课就绪和草稿可用性始终由当前学期事实重新计算。班级、作息、教学任务、资源或排课规则发生相关变化时，排课就绪回到待确认，受影响草稿成为过期课表草稿；不自动删除、复制、合并或改写草稿，已发布课表保持快照不变。

**手工排课录入**：首张课表的班级、作息、科目节数和教师任课由用户在排课工作台中直接维护；科目与教师档案的日常维护在对应步骤的档案视图完成，资源与导入辅助工作面保留组合导入、教室/场地配置和管理员专用的教师登录账号绑定。工作台不因引用缺失而自动创建档案。

**手工批量编辑**：用户在工作台中对已选班级或教学任务执行复制、批量填充周课时或批量指定教师等明确动作。批量动作与单条保存遵循同一学期和权限边界，失败时不产生部分结果。

**手工预排**：在自动排课前对固定、尽量或不排课位以及合班、场室等安排做出的准备动作。结果落在现有规则和课表草稿边界内，不是独立的发布状态。

**未排课程**：当前课表草稿中某项教学任务已排节数低于每周要求的部分。数量由当前课表实时计算；自动排课可以附带本次生成时的诊断原因，但手工调整后该原因不再视为当前事实，发布检查仍以实时数量为准。

**锁定排课**：用户明确确认某个已排课位在后续修复或再次排课中保持不变的安排。锁定后必须先解除才能手工移动或移除；共享同一时段的合班/走班安排作为整体确认。

**排课修复回路**：从生成结果开始，用户查看未排课程和诊断，在课表中手工补排、移动或锁定，再以选定草稿为来源再次排课，最后执行发布检查。手工调整保存在当前草稿中；再次排课默认可调整全部未锁定排课并创建新的结果草稿，不改变来源草稿。

**排课尝试**：一次自动排课请求及其最终状态、诊断和审计记录。成功或提前停止且有可行结果时关联新课表草稿；完整无解、取消或运行失败时不关联结果草稿。

**教学资源按需声明**：教室和场地不是首张课表的默认输入，也不按学段或科目自动推断。教学任务未声明资源需求时不占用教室或场地；声明所需资源类型时由系统安排兼容资源，指定具体资源时固定使用该资源。没有兼容资源是阻断问题，容量不足是提醒；资源总量不足是否允许继续遵循部分排课边界。

**合班/走班安排**：多个使用同一作息时间表的班级组成的共享排课安排。只有一项教学任务时表示合班，多项同步教学任务时表示走班。它由用户在“科目节数”中的高级入口按需创建，不按年级或学段自动生成；走班中的并行教学任务必须具有相同的每周课时和连堂要求。

**连堂要求**：教学任务中经用户明确确认的连续上课要求。科目档案中的默认连堂只提供录入建议，不会自动写入或追溯修改已有教学任务；连堂总量不能超过周课时，并且必须能落在作息时间表的连续常规课时中。

**高级排课约束**：全校禁排、教师时段、固定课位和时段窗口等非日常输入。它们在排课工作台的“开始排课”步骤中以收起摘要和直达维护动作呈现，不新增流程步骤；只有已激活的规则版本参与本次排课，硬约束无可用位置时阻断，软约束只产生提醒。

**排课生成快照**：一次排课运行固定的规则集版本、学期求解配置、教学任务输入和来源草稿。生成结果保留这些依据；运行期间或生成后当前数据发生变化时，结果仍作为旧快照草稿保存，但发布前必须回到相关步骤重新检查并基于当前输入重新生成，不会自动替换现有草稿或已发布课表。

**过期课表草稿**：其排课生成快照与当前学期教学任务或排课规则输入不一致的草稿。过期草稿可以查询、比较和继续手工调整，但不能直接发布；必须先基于当前输入重新检查并重新生成。

**排课草稿生命周期**：前置检查失败时不创建或修改草稿；没有来源草稿时，检查通过并确认就绪后创建一个排课基线草稿。成功求解或提前停止得到的可行结果始终创建新的结果草稿，来源草稿和既有草稿不被覆盖或隐式删除；完整无解、任务失败或取消不创建结果草稿，只保留排课尝试的诊断和审计。用户手工调整结果后，未排课程按当前草稿实时重算；再次运行以选定草稿为来源并保留新版本，用户可按既有权限自行处理不需要的草稿。

**快捷入口**：仪表盘中面向角色的固定工作入口，最多三项，由角色职责决定；教务主任默认进入“排课工作台”、课程表调整和版本与发布。教学任务和自动排课不再提供工作台以外的用户入口，排课规则按工作台上下文进入；不保存个人固定或最近访问设置，也不替代功能分组。

**功能分组**：侧边栏中直接展示的工作空间、排课主流程、日常运行、报表和系统管理五组业务入口。仪表盘和排课工作台位于工作空间；排课主流程由课程表调整、版本与发布和课表查询承接生成后的工作。学期准备能力在排课工作台的辅助工作面中提供，教学任务、自动排课和排课规则不在侧栏中与工作台入口并列，其中前两者只在工作台内提供用户工作面。分组标题用于组织页面，不是需要点击进入的业务页面。

**通知**：统一的消息工作面，包含“我的通知”和面向日常操作角色的“确认看板”两个视图；普通用户只能查看自己的通知。

**系统管理页面**：系统管理员使用的三类独立职责页面：系统管理（学校、通知和排课参数）、备份恢复、账号权限。

## 学期与课表

**学期**：承载学校教学数据并可能对师生发布课表的学年学期。学期中的数据变更需要遵守当前上下文、发布和备份规则。

**当前学期**：学校当前正在进行的唯一工作上下文。仪表盘、快捷入口和默认业务操作围绕当前学期展开，其他学期主要用于历史查询或复制。

**教学任务**：描述教师、班级、科目、每周课时以及连堂、协同教学等排课要求的教学安排。

**教学任务输入快照**：课表生成时所依据的一组教学任务。发布检查读取当前教学任务但不替换该快照；教学任务之后发生变化时，已有课表保持不变，排课流程会提示基于当前任务重新生成草稿。

**课表草稿**：尚未对学校正式生效、可以继续检查和编辑的排课版本。它在排课规则版本确定后单独创建，不由基础数据或教师安排模板导入生成。

**已发布课表**：经过教务主任或内置系统管理员确认，并作为学校当前安排使用的课表版本。

**课表发布**：把课表草稿转为学校当前可查询安排的业务动作；发布不等于备份，也不等于审批。

**草稿可见范围**：教务主任和内置系统管理员可以查看、编辑课表草稿并执行批量或版本级导出；教师只使用已发布课表，并可查询、导出单张已发布课表。

**归档学期**：不再作为当前教学上下文、默认只读的历史学期。归档学期可以查询或复制，但不能进行常规排课写入或发布。

## 排课规则

**排课规则**：约束教学任务、教师、班级、科目和教室资源在课表中的时间、位置、数量或相互关系。排课规则不等同于教学任务；教学任务描述“要上什么课”，排课规则描述“这些课怎样安排”，教师安排模板导入不导入或激活排课规则。

**规则模板**：一类可以被系统理解、校验并用于排课的规则类型，例如禁排时段、时段偏好、每周课时上限、连堂和科目分布。规则只有选择了受支持的模板并填完必要条件后，才可以参与排课。

**规则候选**：从人工录入、Word 或 Excel 导入中整理出的待确认规则。规则候选保留原文和来源；未确认、无法表达或存在冲突的候选不会改变已发布课表。

**规则集版本**：一次排课或课表草稿所使用的完整规则集合。规则修改通过建立新版本生效，历史版本保持不变，以便解释和重现已经生成或发布的课表。

**规则强度**：规则对排课的要求程度，分为必须满足、尽量满足和仅作记录。必须满足的规则不能违反；尽量满足的规则用于比较候选课表；仅作记录的内容不参与求解，但应在检查报告中可见。

**规则来源**：规则最初来自人工录入、Word、Excel 或系统默认值的来源说明。来源原文和定位信息用于复核规则含义，不替代规则的结构化表达。

## 角色与权限

**内置系统管理员**：系统首次初始化时建立、唯一承担系统管理员职责的账号；系统管理员职责不属于可授予其他账号的角色组合。

**固定角色**：系统只使用系统管理员、教务主任和教师三种稳定职责。系统管理员仅属于唯一内置账号；普通账号可以拥有教务主任、教师或两者的组合。

**管理视角**：拥有教务主任职责的多角色用户登录后的默认工作视角。多角色用户的权限取并集，不通过切换视角临时获得或失去权限。

**教务主任**：负责除系统管理外的全部学校教务业务，包括学校与学期准备、基础数据、教学任务、排课规则、自动与手动排课、课表发布、导出、请假、调课、代课、通知和统计。当前学校暂设一名教务主任，但系统不强制账号数量。

**教师**：使用已发布课表、登记本人请假、确认本人通知并查看本人统计的角色，不编辑教学任务或排课草稿。

**动作权限**：对查看、编辑、导入、发布、导出、备份、恢复等具体业务动作的授权，而不仅是能否打开某个页面。

**数据范围**：角色可以作用的数据边界，例如本人、负责范围或全校。教师可以查看已发布的全校课表，但只能修改本人业务数据；教务主任通常使用全校查看范围。

**管理员专属动作**：只有内置系统管理员可见和执行的系统级操作，包括账号与角色管理、教师账号绑定、学校与 SMTP 设置、备份恢复、全局审计、核心数据永久或级联删除以及管理员恢复。

**高风险动作**：会改变学校正式安排、数据安全或账号安全的动作，包括课表发布、核心数据的永久或级联删除、备份恢复和账号权限变更。这些动作需要保留确认和审计要求。课表发布由教务主任或内置系统管理员执行；其余系统级高风险动作仅限内置系统管理员。特殊日期条目维护、草稿课表格移除、代课处理撤回和排课准备确认撤回属于可重建的日常操作，不属于此处的核心数据删除，仍遵循各自的业务角色、当前学期只读边界与操作审计。
