# 学校教务排课上下文

本项目服务单校教务团队，覆盖从学期准备、排课发布到学期中调课与代课的完整工作流。本文记录项目中必须保持一致的业务词汇，不记录页面或代码实现细节。

## 使用路径

**设置向导**：新管理用户建立学校排课基础的四步流程，依次完成学校与学期、基础数据、作息安排和完成检查。向导可以保存并退出；完成设置不等于已经建立教学任务或完成排课准备。

**中性学期**：仅包含学年、学期和真实起止日期的学期外壳。创建时不根据学校或班级学段自动写入科目、教师、班级或作息。

**组合基础数据导入**：把科目、教师、班级和教室/场地放在同一工作簿中，先展示新增、未变化、将修改和冲突，再由用户确认并在一个事务中写入。教师登录账号不属于这次导入。

**作息分组**：一组共用同一套作息时间表的班级。作息分组可以参考班级的学制标签，但学制标签不决定分组，且同一学制可以拆成多组。

**完成检查**：根据当前学期真实数据计算的向导检查。缺少有效日期、科目、教师、班级、常规课节次或存在未解决冲突时阻止完成；缺少教室、教师账号、特殊日期或铃声时间时提醒并要求用户确认。

**基础设置完成**：学期、基础数据和至少一套可排课作息已经满足向导的最低条件，用户可以开始建立教学任务；它不是课表发布或排课准备确认。

**快捷入口**：仪表盘中面向角色的固定工作入口，最多三项，由角色职责决定；不保存个人固定或最近访问设置，也不替代功能分组。

**工作空间**：面向系统管理员、排课管理员和教务主任的当前学期运行总览，用于发现重点事项并进入对应业务流程。工作空间不取代面向所有角色的仪表盘，也不产生独立于现有业务数据的任务或建议。

**功能分组**：侧边栏中直接展示的工作空间、学期准备、排课主流程、日常运行、报表和系统管理六组业务入口。分组标题用于组织页面，不是需要点击进入的业务页面。

**通知**：统一的消息工作面，包含“我的通知”和面向日常操作角色的“确认看板”两个视图；普通用户只能查看自己的通知。

**系统管理页面**：系统管理员使用的三类独立职责页面：系统管理（学校、通知和排课参数）、备份恢复、账号权限。

## 学期与课表

**学期**：承载学校教学数据并可能对师生发布课表的学年学期。学期中的数据变更需要遵守当前上下文、发布和备份规则。

**当前学期**：学校当前正在进行的唯一工作上下文。工作空间、仪表盘、快捷入口和默认业务操作围绕当前学期展开，其他学期主要用于历史查询或复制。

**教学任务**：描述教师、班级、科目、每周课时以及连堂、协同教学等排课要求的教学安排。

**课表草稿**：尚未对学校正式生效、可以继续检查和编辑的排课版本。

**已发布课表**：经过排课管理员确认并作为学校当前安排使用的课表版本。

**课表发布**：把课表草稿转为学校当前可查询安排的业务动作；发布不等于备份，也不等于审批。

**草稿可见范围**：系统管理员和排课管理员可以编辑课表草稿，教务主任可以查看草稿但不能编辑，教师只使用已发布课表。单张已发布课表可供登录用户查询和导出，批量或版本级导出属于管理操作。

**归档学期**：不再作为当前教学上下文、默认只读的历史学期。归档学期可以查询或复制，但不能进行常规排课写入或发布。

## 角色与权限

**固定角色**：系统管理员、排课管理员、教务主任和教师四种稳定的职责集合。一个用户可以同时拥有多个固定角色。

**管理视角**：拥有排课管理职责的多角色用户登录后的默认工作视角。多角色用户的权限取并集，不通过切换视角临时获得或失去权限。

**排课管理员**：负责维护教学任务、编辑排课草稿、运行排课和发布课表的业务角色。

**教务主任**：负责全校监督和日常教务运行的角色，可以查看全校数据并处理有限的校历、请假、调课与代课事务，但不负责核心排课编辑或课表发布。

**教师**：使用已发布课表、登记本人请假、确认本人通知并查看本人统计的角色，不编辑教学任务或排课草稿。

**动作权限**：对查看、编辑、导入、发布、导出、备份、恢复等具体业务动作的授权，而不仅是能否打开某个页面。

**数据范围**：角色可以作用的数据边界，例如本人、负责范围或全校。教师可以查看已发布的全校课表，但只能修改本人业务数据；教务主任通常使用全校查看范围。

**高风险动作**：会改变学校正式安排、数据安全或账号安全的动作，包括课表发布、核心数据的永久或级联删除、备份恢复和账号权限变更。这些动作需要保留确认和审计要求。特殊日期条目维护、草稿课表格移除、代课处理撤回和排课准备确认撤回属于可重建的日常操作，不属于此处的核心数据删除，仍遵循各自的业务角色、当前学期只读边界与操作审计。
