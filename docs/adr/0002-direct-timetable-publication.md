# 排课管理员直接发布课表

状态：已接受。

排课管理员负责生成、检查并发布课表，教务主任负责查看和监督；当前不引入发布审批流。这个选择符合学校现有的线下责任分工，也避免为了审批状态、撤回、锁定和通知时机引入一套会阻塞首次成功的额外工作流。课表发布仍属于高风险动作，必须经过完整性检查、明确确认并留下审计记录。

## 后果

- “生成课表”和“正式发布”必须在界面上明确区分。
- 教务主任的监督体验应通过仪表盘、版本记录和统计完成，而不是默认增加审批步骤。
- 如果未来出现明确的多级审批需求，应作为独立工作流重新建模，不能偷偷把查看权限改成审批权限。
