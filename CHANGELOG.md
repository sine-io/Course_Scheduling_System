# 变更记录 Changelog

本文件记录各版本的重要变更，格式参考 [Keep a Changelog](https://keepachangelog.com/en/1.1.0/)，版本号遵循 [Semantic Versioning](https://semver.org/)。

破坏性变更(需人工介入才能升级)以 ⚠️ 标注。

> **新用户请直接安装 v1.2.0**(见 [README](README.md) 快速开始)。v1.0.0 与 v1.1.0 是开发过程中的里程碑版本,以下条目保留作为变更记录。

## [未发布]

### 移除
- 设置向导不再提供示例体验/正式建校路线或学校级学制模板，改为“学校与学期、基础数据、作息安排、完成检查”四步中性流程。
- 移除内置示例学校数据、示例学期标记及相关接口；未部署阶段生成的本地示例学期由迁移直接清理。
- 移除独立的首次成功状态、仪表盘引导列表和侧栏设置向导入口；仪表盘无学期空状态与系统管理仍可进入向导。

## [1.2.0] — 2026-08-02

本版本继续保持单校自建和简体中文定位，没有数据库破坏性变更。升级时请同步更新 `docker-compose.yml` 与 `.env.example`。

### 新增
- **学校信息可配置**：系统管理员可以在“系统管理 → 学校信息”修改学校名称，课表、导出文件和通知会统一使用新名称。
- **国内初中示例数据**：向导和系统管理可加载一套虚构的初中示例，包含 18 个班、49 位教师、252 条教学任务和 16 个科目，用于快速体验排课、调课与代课、导出和备份流程。示例数据仅用于演示，不代表政策或真实学校。
- **教学任务超课时上限**：可按教师设置允许的超课时数量；创建或修改教学任务时会立即提示超出上限的情况，导入数据也会执行同样检查。
- **Linux/macOS/NAS 与 Windows 一键安装脚本**：支持自定义安装目录、端口、Compose 项目名称、镜像版本和只生成配置文件。检测到同名项目位于其他目录时会先要求确认，避免误接管已有数据库卷。
- **离线 HTML 文档**：部署和开发 Markdown 文档可由 `scripts/build_docs.py` 生成带目录、主题切换和响应式布局的离线页面；CI 会检查 HTML 是否与 Markdown 同步。

### 修正
- 修复 Web 镜像跨架构构建时的原生依赖问题，使用锁文件执行 `npm ci`，确保构建可复现。
- 补充迁移与架构文档同步检查、安装文档和 Windows/Linux 部署说明。

## [1.1.2] — 2026-07-16

### 修正
- 修复首次登录修改密码页面的重复提交问题，并补充 Playwright 回归测试，避免一次操作发送两次请求。

## [1.1.1] — 2026-07-14

### 修正
- **「系统管理」整页打不开**(v1.0.0 起就存在的缺陷):页面只剩左侧菜单,右边一片空白——备份、恢复、SMTP 通知设置、重新启动设置向导**四项功能全都点不进去**。原因是前端少挂一个对话框组件的容器(`n-dialog-provider`),页面初始化时直接抛出异常而整页渲染不出来。
  - 这一页先前**没有任何端对端测试覆盖**,所以缺陷一路溜过 v1.0.0 与 v1.1.0。本版补上该页的回归测试(卡片渲染 + 立即备份 + 删除备份),日后不会再无声坏掉。

### 文件
- 操作手册 10 张截图全部重拍(先前是旧主色,且与现状不符);截图生成器改为自备示范数据,任何人都能用一套空的 Docker 栈一键重拍。
- README 补上实际页面;部署文件改以「全新安装」为主轴。

## [1.1.0] — 2026-07-14

**加固版。** 针对 v1.0.0 发行后的体检结果,补上单校长期使用最会咬到人的几处:排课不再堵住导出、部分排课不再整锅失败、排不下的原因不再消失、班名不再重复。无数据库破坏性变更(迁移自动执行),但 ⚠️ **升级时必须连 `docker-compose.yml` 一起更新**(新增 `worker-ops` 容器)。

官方镜像(amd64 + arm64 双架构)已发布于 GHCR:
`ghcr.io/sine-io/course_scheduling_system-{api,worker,web}:v1.1.0`

### 新增
- **后台任务分两条队列,排课不再堵住导出**(⚠️ 新增 `worker-ops` 容器):自动排课走 `default` 队列、导出/备份/恢复/发送邮件与定时任务走 `ops` 队列,各由一个 worker 进程监听。**在等自动排课的那几分钟里,按「导出课表」「立即备份」仍然是立即响应的**(先前会排在排课后面直到超时失败)。
  - ⚠️ **升级注意**:请连同 `docker-compose.yml` 一起更新到新版(新增了 `worker-ops` 服务)。只换镜像、沿用旧 Compose 文件的话,系统会照常启动、排课也正常,但**导出、备份、恢复、发送邮件会失败,每日自动备份也会停摆**——因为没有进程在守 `ops` 队列。见[升级指南](docs/deploy/upgrade.md)。
  - 真的漏更新 Compose 文件时,**系统会立刻告诉你**:按导出/备份会立即响应一句「worker-ops 没有在执行……请更新 docker-compose.yml」,而不是让你等一分半的超时再看到一句看不懂的错误;api 启动时也会在 log 留下同样的警告(每日自动备份停摆是**无声**的,不主动讲就没人会发现)。
  - 排课进行中仍会**拒绝恢复**(409):pg_restore 覆盖整个数据库,而排课正要把结果写回同一个库——这是数据安全,不是排队。

### 安全与无障碍
- **`/api/docs` 正式部署默认关闭**:端点本身均受权限保护,公开它不是漏洞,但没必要把整套内部 API 摊在网络上(尤其是有公开域名的部署)。需要对接时可在 `.env` 设 `API_DOCS_ENABLED=true` 打开。
- **主色调深,按钮文字达 WCAG AA**:原本的绿配白字对比只有约 3.4:1,未达正常文字要求的 4.5:1;调深后为 5.4:1(色系不变,整体设计不受影响)。

### 修正
- **同学期不再允许两个同名班级**:冲突信息、课表、导出全都以班名指称班级,两个「301」会让人根本分不出是哪一班。⚠️ 升级时若现有数据已有重复班名,系统会自动把后面的改成「301 (2)」这样的名字(**不会删除任何数据**),请升级后到「基础数据 → 班级」确认并改成正确的班名。
- **排课无解分析中按「取消」现在真的会停**:先前那段分析最长要跑一分钟且完全不理会取消,用户只能干等,最后还收到一份他已经不想要的报告。
- **调课与代课记录与请假列表加上查询条数上限**:整学年不筛选地查会一次拉出数千条。现在取最新的 1000 条,**并在页面上明讲被截断了**(不讲的话,你会以为这学期就只有这些记录);要看更早的请缩小日期区间。
- **开新学期复制漏了两样东西**:现在复制对话框可填新学期的**起止日**(默认带入来源学期往后推半年,请依实际校历确认),并可勾选一并带走**排课偏好设置**(软约束权重)。先前起止日是空的、偏好设置会悄悄回到默认值——前者会让请假展开与今日看板的日期判定失准,而页面上看不出哪里不对。
- **部分排课遇到「完全排不下的课」会整个失败**:例如协同教学的两位教师不可排时段刚好盖满整周。现在该门课会列进未排列表并说明原因(「找不到任何可排的 1 连堂时段」),其他课照排——这正是部分排课存在的意义。
- **走班群组的「未排 N 节」重复计数**:群组是多门选修同时段开,少排一个时段就是少一节课,先前会乘上群组内的课程数量。
- **排不下的原因不再消失**:未排列表随草稿存进数据库(先前只在 Redis 保留 24 小时),草稿发布后仍能在版本页的完整性报告看到每门课「为什么排不下」。
- **导出/备份在新环境下超时失败**:RQ 取结果原以阻塞 XREAD 等待,在 redis-py 8 下会被 socket 超时打断(worker 已完成渲染,API 端却回 500)。改为轮询取结果,不再依赖阻塞读的唤醒行为。
- **恢复成功后,系统日志仍会出现一段红色错误**:恢复本身、数据与页面都是正确的,但 `pg_restore` 会中止数据库上的所有连接(包含这个请求自己的),连接收尾时就在日志留下一段看似崩溃的信息。刚按下「恢复」的人去看日志想确认成功与否,只会被这段红字吓到——现在恢复前会先妥善收掉自己的连接,日志干净。

### 内部
- 测试日期统一由执行当日推算,不再硬编未来日期(硬编的日期会在成为过去后让调课与代课测试整套失效)。
- 后端核心依赖全部钉上主版号上限(先前只有下限:某次重建装到 redis-py 8,导出/备份就在新环境全数超时失败——镜像每次发行都重新构建,不钉上限等于把用户的部署交给上游的发版节奏)。
- 开发用 `docker-compose.dev.yml` 的 worker 同时守两条队列(先前没有任何进程在守 `ops`,dev 环境的导出、备份、发送邮件与定时任务其实全都不会动)。
- 将 31 项 E2E 回归测试纳入 CI：每次 PR 都会启动完整 Docker 全栈；E2E 未通过时不发布镜像。

## [1.0.0] — 2026-07-12

**首次公开发行。** 完整涵盖一所学校从基础数据构建、教学任务、手动与自动排课、发布,到学期中调课与代课、导出与备份的全流程。

官方镜像(amd64 + arm64 双架构)已发布于 GHCR:
`ghcr.io/sine-io/course_scheduling_system-{api,worker,web}:v1.0.0`

### 安全与发行前强化
- **SECRET_KEY 防呆**:未设置(仍为默认/示例值)时自动改用随机密钥并警告,避免以公开密钥签署 session cookie。
- **HTTPS 自动安全 cookie**:设置 `SITE_ADDRESS` 域名(启用 HTTPS)时,登录 cookie 自动加上 `Secure` 标记。
- **请求体上限**:Caddy 限制 200MB,避免超大上传耗尽单校主机内存。
- **后台任务韧性**:排课进行中禁止恢复(避免数据库被无预警覆盖);阻塞式任务超时即取消;每日备份链具自我续期与心跳自愈。
- **恢复容错收紧**:`pg_restore` 仅容忍跨版本设置参数噪音,其余错误统一视为失败(避免数据缺漏被误报为成功);可忽略警告显示于界面。
- 新增 [THIRD-PARTY-NOTICES.md](THIRD-PARTY-NOTICES.md)(第三方组件授权说明,均与 MIT 兼容)。
- 新增[排课管理员操作手册](https://sine-io.github.io/Course_Scheduling_System/)(11 章网页)。

### 部署与发行(M5-3)
- `docker-compose.yml` 同时支持两种部署：从源代码执行 `sudo docker compose up -d` 构建，或执行 `sudo docker compose pull` 拉取官方预建镜像；镜像版本由 `.env` 中的 `IMAGE_TAG` 控制，正式部署建议固定版本。
- Caddy 站点地址改由环境变量 `SITE_ADDRESS` 决定:默认内网 HTTP;设为域名即自动申请并续期 Let's Encrypt HTTPS 证书(证书存于 `caddydata` volume)。
- 新增中文部署手册 `docs/deploy/`:安装(Windows/Linux/NAS)、升级、备份与异地备份、域名与 HTTPS、常见问题。
- 新增 `CHANGELOG.md`、`CONTRIBUTING.md`;改写 README(含英语摘要与功能总览)。

### 报表、备份与导出(M5-0 ~ M5-2)
- **课表导出**:班级、教师和教室/场地课表可导出为 Excel、PDF(WeasyPrint,内嵌 Noto CJK 中文字体,A4)或 PNG;另支持全校总表 Excel 和全部班级批量导出 zip。
- **备份与恢复**:每日自动 `pg_dump`(可设置时刻与保留份数,默认 02:00 / 30 份、自动轮替);系统管理界面立即备份、下载、上传恢复;恢复前自动备份现状、恢复后强制全员重新登录(以 Redis 记录 session 时效,独立于被恢复的 PostgreSQL)。
- **发行前置**:多阶段 Docker 镜像(api 精简 / worker 含 PDF 字体与 PostgreSQL client);RQ 定时任务调度骨架(自我续期心跳);60 班规模性能 fixture。
- 重新发布课表后,对「今日之后」仍指向旧单元格的受影响节次于发布时提出警告(条件 D 最小防护)。

### 调课与代课(M4)
- 请假登记与受影响节次展开(快照保存,不随课表改版漂移;半天/多天/跨周假、六日制学校支持)。
- 调课与代课处理工作台与代课推荐引擎（同科目 > 当天已在校 > 本月代课课时少，并附易懂的推荐理由）；调课（swap）包含四项验证；指派后立即生效（不设置邀请或婉拒流程）。
- 通知系统:站内(铃铛、未读数)+ Email(SMTP 选配,经 `NotificationChannel`,事务提交后才发送);教师「确认收到」;排课管理员看板未确认者一键再提醒。
- 今日调课与代课看板、A4 公告打印、历史查询;月结代课课时统计(代课节数 vs 计费节数,跨月假单自动拆月,Excel 导出,教师可查本人)。

### 自动排课(M3)
- 三套学制验证数据集(小学/初中/中职)。
- OR-Tools CP-SAT 排课引擎:solver 数据层与 pre-flight 检查、H1–H10 硬约束建模、S1–S8 软约束加权目标;连堂、走班同步、锁定单元格、跨作息时间表墙钟重叠建模。
- Worker 后台求解包含实时进度、提前结束时保留当前最佳解、超时可设置;结果写回新草稿(锁定单元格固定、现有单元格作 hint)。
- 无解冲突定位:以删除法逐类探测,转译为教务语言建议;部分排课模式(可放宽的约束转高权重软约束,未排入者列列表)。

### 教学任务与手动排课(M2)
- 教师账号绑定与联系信息;教学任务管理(走班群组、协同教学、连堂)与课时实时统计。
- TimetableGrid 拖拽周课表组件;冲突检查服务与手动排课 API(单格 <100ms,跨作息时间表以墙钟时间重叠判定)。
- 排课工作台（三种视角、草稿自动保存、撤销/重做）；支持多草稿版本管理与发布，已发布版本为不可变快照。

### 基础数据(M1)
- 学期与作息时间表(五种学制模板、同学期多套作息时间表);教师/班级/科目/教室/场地 CRUD(含引用完整性)。
- Excel 导入(逐行验证、事务性入库);首次登录设置向导;开新学期复制向导;混合学制班级↔作息时间表指派。

### 项目骨架(M0)
- Docker Compose 五容器骨架与开发热重载设置;账号、bcrypt 登录、session cookie 与 RBAC(admin/director/scheduler/teacher);首次登录强制改密。
- CI:ruff + mypy + pytest / eslint + vue-tsc + build + vitest / PostgreSQL 迁移验证 / 双架构镜像构建发布。

[Unreleased]: https://github.com/sine-io/Course_Scheduling_System/compare/v1.2.0...HEAD
[1.2.0]: https://github.com/sine-io/Course_Scheduling_System/compare/v1.1.2...v1.2.0
[1.1.2]: https://github.com/sine-io/Course_Scheduling_System/compare/v1.1.1...v1.1.2
[1.1.1]: https://github.com/sine-io/Course_Scheduling_System/compare/v1.1.0...v1.1.1
[1.1.0]: https://github.com/sine-io/Course_Scheduling_System/compare/v1.0.0...v1.1.0
[1.0.0]: https://github.com/sine-io/Course_Scheduling_System/releases/tag/v1.0.0
