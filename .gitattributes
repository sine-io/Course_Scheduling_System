# 统一换行为 LF,确保 shell 脚本与 Dockerfile 在 Linux 容器内可正确执行
* text=auto eol=lf

# 明确指定必须为 LF 的文件(容器内执行)
*.sh text eol=lf
Dockerfile* text eol=lf
*.py text eol=lf

# 二进位档不做换行转换
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.woff binary
*.woff2 binary
